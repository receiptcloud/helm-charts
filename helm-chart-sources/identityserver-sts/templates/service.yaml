apiVersion: v1
kind: Service
metadata:
  name: {{ include "identityserver-sts.fullname" . }}
  labels:
{{ include "identityserver-sts.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "identityserver-sts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
