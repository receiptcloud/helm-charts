apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identityserver-sts.fullname" . }}-test-connection"
  labels:
{{ include "identityserver-sts.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "identityserver-sts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
